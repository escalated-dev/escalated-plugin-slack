{
  "name": "@escalated-dev/plugin-slack",
  "version": "0.1.0",
  "description": "Slack notifications for ticket events — TypeScript SDK version",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@escalated-dev/plugin-sdk": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
